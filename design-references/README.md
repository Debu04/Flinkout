# Flinkout Design References

These files are the responsive UI source of truth supplied for the Flinkout redesign.

## Desktop

- `flinkout_hybrid_feed_desktop` → `/`
- `interactive_session_post` → `/activities/[id]`
- `start_movement_flow` → `/record` before recording
- `live_session_tracking_active` → `/record` while recording

## Mobile

- `flinkout_hybrid_feed_search_messages_nav` → `/`
- `search_discovery_mobile` → `/explore`
- `live_activity_map_local_trails_radius` → `/map`
- `explorer_profile_public` → `/u/[username]`
- `session_detail_mobile` → `/activities/[id]`
- `start_movement_mobile` → `/record` before recording
- `live_tracking_mobile_with_header_activity` → `/record` while recording
- `messages_mobile` → `/messages`

The original `screen.png`, `code.html`, and shared `DESIGN.md` files are retained unchanged.
